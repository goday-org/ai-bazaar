# W1 总结 — 2026-05-27

## 完成的任务

- [x] 接受协作邀请，clone 仓库，创建 `strip/w1-baseline` feature 分支。
- [x] 按 `docs/HANDOFF.md §0` 顺序读完 8 份交接文档，并在 issue #6 完成 onboarding 审批。
- [x] 导入 Wei-Shaw/sub2api backend baseline 到 `seller-relay/`，保留 LGPL-3.0 LICENSE 和 fork attribution。
- [x] 验证 baseline 可编译后执行 `SUB2API_STRIP.md` Step 1 删除 admin/auth/user/payment routes。
- [x] 执行 Step 2 删除 admin/auth/payment/user-facing handlers，并刷新 Wire 生成输出。

## 未完成 / 延期

- [ ] W2 service/repo/schema 剪枝，原因：ROADMAP.md 将 Step 3-8 明确排入 W2。

## 关键决策（DEVIATION）

- **commit `ae0e664`**
  - DEVIATION: `SUB2API_STRIP.md §2.1` imports the backend baseline byte-for-byte.
  - CHANGE: Redacted upstream public GOCSPX fallback client_secret literals and updated matching tests while preserving env-based configuration.
  - REASON: Project hard rules forbid committing GOCSPX secrets to git history.

- **commit `6ef7ff9`**
  - DEVIATION: `SUB2API_STRIP.md` Step 2 only calls out `internal/handler/wire.go` changes.
  - CHANGE: Also removed stale generated/test wiring, moved the gateway `/v1/usage` date helpers into a retained gateway helper file, and split dummy PEM test markers so forbidden private-key block literals are not present in source.
  - REASON: The repository keeps generated Wire output checked in, gateway `/v1/usage` still depends on those helpers, and hard rules prohibit committing private-key-looking literals.

## 已知问题

- Running `go run github.com/google/wire/cmd/wire` required `-mod=mod`, which added the missing `github.com/google/subcommands v1.2.0` go.sum entries.

## 验收命令输出

```bash
$ cd seller-relay
$ HTTP_PROXY=http://127.0.0.1:7897 HTTPS_PROXY=http://127.0.0.1:7897 ALL_PROXY=socks5://127.0.0.1:7897 GOTOOLCHAIN=auto GOPATH=/private/tmp/ai-bazaar-gopath GOMODCACHE=/private/tmp/ai-bazaar-gomodcache GOCACHE=/private/tmp/ai-bazaar-gocache go build ./...
# no output

$ HTTP_PROXY=http://127.0.0.1:7897 HTTPS_PROXY=http://127.0.0.1:7897 ALL_PROXY=socks5://127.0.0.1:7897 GOTOOLCHAIN=auto GOPATH=/private/tmp/ai-bazaar-gopath GOMODCACHE=/private/tmp/ai-bazaar-gomodcache GOCACHE=/private/tmp/ai-bazaar-gocache go vet ./...
# no output

$ git log --oneline -- seller-relay | wc -l
       3

$ HTTP_PROXY=http://127.0.0.1:7897 HTTPS_PROXY=http://127.0.0.1:7897 ALL_PROXY=socks5://127.0.0.1:7897 GOTOOLCHAIN=auto GOPATH=/private/tmp/ai-bazaar-gopath GOMODCACHE=/private/tmp/ai-bazaar-gomodcache GOCACHE=/private/tmp/ai-bazaar-gocache go test ./internal/handler ./internal/server/routes ./cmd/server
ok  	github.com/Wei-Shaw/sub2api/internal/handler	19.689s
ok  	github.com/Wei-Shaw/sub2api/internal/server/routes	(cached)
ok  	github.com/Wei-Shaw/sub2api/cmd/server	(cached)
```

## 下周计划

- W2: execute `SUB2API_STRIP.md` Step 3-8 for service/repo/schema pruning and regenerated Wire/Ent output.

## Reviewer 答复

- GOCSPX redaction accepted as-is.
- Step 2 deviations accepted: stale Wire cleanup, `/v1/usage` retention, and PEM marker splitting.
- `/v1/usage` dependent helpers deferred to W2 Step 3-6 prune.
